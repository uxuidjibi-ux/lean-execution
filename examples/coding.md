# Coding Example

Lean Execution: inspect the failing area and direct dependencies, make the smallest defensible fix, run relevant verification, then report the material delta and blocker if any.

Example:
`DONE: Fixed the null-state regression and relevant tests pass. NEXT: Continue with the next failing acceptance case.`
